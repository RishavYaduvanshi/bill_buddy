type Mutation{
    addUser(
        username:String!,
        password:String!,
        first_name:String!,
        last_name:String!,
        email:String!,
        age:Int!
    ):User!

    loginUser(
        email:String!,
        password:String!
    ):User!
}
